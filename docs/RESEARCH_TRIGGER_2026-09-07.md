# Research Workflow Trigger

This file intentionally triggers the local-only research benchmark workflow on `research/p1-evaluator-hardening`.

It contains no benchmark logic and no credentials.

**Execution rule:** iterative benchmark execution must use local Ollama only; Azure is reserved for an explicitly authorized final run.
