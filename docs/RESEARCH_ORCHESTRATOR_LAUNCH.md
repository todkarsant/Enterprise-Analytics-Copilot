# P1 Research Orchestrator Launch

This commit is the explicit unattended-cycle launch marker for the corrected chunked orchestrator.

The run must use remote Ollama only, preserve the pinned Spider evaluator and frozen unseen-schema split, retain raw traces, and stop at the P5 research gate for human review.
