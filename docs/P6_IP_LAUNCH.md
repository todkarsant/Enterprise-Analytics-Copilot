# P6-IP Controlled Experiment Launch

This marker authorizes the local-Ollama P6-IP controlled experiment on the development and held-out-schema partitions under the frozen benchmark contract.

No Azure/paid LLM is authorized by this marker. The workflow must enforce the local-only contract.
